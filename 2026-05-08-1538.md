# 2026-05-08-1538

我现在想整体梳理一遍这个项目，尤其是前端模块涉及哪些数据。由于当前来自各渠道的数据量很大，后续规划是让各类数据都能按统一规则接入，最终在前端集中呈现，因此必须搭建一个数据中台。中台底层对接各渠道数据源，而这些数据的获取方式各不相同，这里有着极大的DIY和拓展空间。
我们现在会对前端汇总后的数据做 reranker，或用大语言模型提炼或聚类事件，并调用库中的内容进行对话。

我现在正在规划AI对话模块，希望接入Hermes，使其具备执行操作的权限。关于AI部分，我打算将个人使用版本与公共版本分离：公共版本将不接入Hermes，具体来说，现在 mobile 版本已做了一些页面规划，你可以查看那部分代码。所以，我们的数据调用和读取模块是一个关键模块。我希望未来能做一些架构上的调整，但眼下还没想好，因为目前主要还是为自己个人使用而开发。不过未来可能希望将这套架构开源，让更多的开发者可以自行 DIY。


现在想把中间的数据层抽象出来，类似于搭建一个数据中台，无论是后续的移动端还是 PC 端，都能统一获取这部分数据。上游的数据抓取工具，所有数据最终都会汇聚到这里。
我想了想这个中间的数据层，希望你先出一份PRD，明确当前的现状，比如有哪些数据渠道，以及这些数据的输入和输出方式。目标是让数据中台实现端到端闭环：外部数据进来后，在中台统一格式，转化为标准化数据，后续无论是移动端还是PC端调用，都能直接复用。在上游的数据获取渠道，如新闻、阿里云服务器上的新闻、MacBook上的B站视频转写等模块，我后续希望将它们分装成独立的小工具，互不干扰。但这些模块获取的数据必须统一接入数据中台。数据中台的另一职责是利用空闲冗余数据进行rank或事件识别。


这个数据中台，目前先不考虑额外接入其他渠道。PRD 里可以简单写一下未来规划，没问题。除了 B 站视频转写文稿和新闻源数据外，我现在还会把个人项目的部分信息，按当前前端的数据结构梳理后输入到 Clawphone Task 模块——该模块直接对应我输入的内容。因此，核心问题是：若未来有其他数据源接入，我们需要制定怎样的标准，要求提供方以何种格式提交数据才算规范？


当前亟需明确数据中台的定位，而非过早罗列具体字段——那样只会让信息过载、难以消化，且后续极易调整。数据中台的核心能力，是统一管理现有数据渠道：既能从这些渠道主动获取数据，也能接收渠道主动推送的数据。本质上，它是一个统一的数据库，支持多源、多表、多字段的数据接入，但关键在于：我们必须清晰掌握当前连接了哪些数据渠道，以及未来接入新数据时，必须遵循哪些统一规范。我们的数据模块在后台运行时，会执行去重、使用LLM模型进行识别与总结，以及通过Rank进行重排序等任务。若由前端或其他外部系统调用获取数据，数据库将提供当前库中所含数据的完整说明。
所以我们得调整 Data Hub 的 PRD 结构：数据获取层要简明扼要，说人话，别写太复杂；数据管理维护层和数据调用层，这三层基本构成了我们的数据中台。如果觉得有必要，也可以补充具体的数据存储部分。